# Searching-a-substring
endswith(substring)
startswith(substring)
count(substring)
Example:
name="python"
print(name.endswith("thon"))
print(name.startswith("yth"))
print(name.count("o"))
Output:
True
False
1
